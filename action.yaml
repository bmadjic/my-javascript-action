name: 'Hello World'
description: 'Greet someone and record'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  time: #id of output
    description: 'The time we greeted you'
  issue_name:
    description: 'name of the new issue'
runs:
  using: 'node12'
  main: 'index.js'
